require "spec_helper"

RSpec.describe Jekyll::Series do
  it "has a version number" do
    expect(Jekyll::Series::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
